# ============================================
# Vault — Environment Variables
# ============================================
# bot, frontend, scripts 全��がこのファイルを参���
# リモートでもローカルでもこの1���ァイルで管理
#
# cp .env.example .env && chmod 600 .env

# --- Anthropic API (AI Advisor) ---
# 取得元: https://console.anthropic.com/ → API Keys
ANTHROPIC_API_KEY=

# --- Bot API (frontend proxy & advisorテスト用) ---
BOT_API_URL=http://localhost:3000
# Usually set this to the same value as API_AUTH_TOKEN.
BOT_API_TOKEN=
# Optional dashboard/proxy auth. When set, the frontend requires Basic or Bearer auth.
FRONTEND_API_SECRET=

# --- Perp Exchange ---
PERP_EXCHANGE=binance

# --- Binance Futures ---
# API key with Futures trading permission ONLY (no withdrawal)
# Restrict to VPS IP address in Binance API settings
BINANCE_API_KEY=
BINANCE_API_SECRET=

# --- Solana ---
# Helius RPC endpoint (includes API key in URL)
HELIUS_RPC_URL=https://mainnet.helius-rpc.com/?api-key=YOUR_KEY
# Wallet keypair (base58-encoded 64-byte Ed25519 keypair)
SOLANA_PRIVATE_KEY=
SOLANA_WALLET_ADDRESS=

# --- Telegram Alerts ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# --- API Server Auth ---
# Generate with: openssl rand -hex 32
API_AUTH_TOKEN=
# Optional exact origin allowed to call the bot API from a browser.
# Leave empty when the bot stays on localhost behind the frontend proxy.
API_ALLOWED_ORIGIN=

# --- Jupiter API ---
# Free tier at https://portal.jup.ag
JUPITER_API_KEY=

# --- Runtime ---
NODE_ENV=production
API_PORT=3000
LOG_LEVEL=info
