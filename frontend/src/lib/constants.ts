// FR thresholds (annualized %)
export const FR_ENTRY_THRESHOLD = 10;
export const FR_EXIT_THRESHOLD = 0;
export const FR_EMERGENCY_THRESHOLD = -10;

// Polling intervals (ms)
export const POLL_FAST = 30_000;   // 30s for status, performance, events
export const POLL_SLOW = 60_000;   // 60s for pnl, fr, snapshots
